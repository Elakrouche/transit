<form method="post" action="{{ path('app_remorque_delete', {'id': remorque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remorque.id) }}">
    <button class="btn">Delete</button>
</form>
