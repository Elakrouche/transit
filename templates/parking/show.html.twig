{% extends 'base.html.twig' %}

{% block title %}Parking{% endblock %}

{% block body %}
    <h1>Parking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parking.id }}</td>
            </tr>
            <tr>
                <th>AdressParking</th>
                <td>{{ parking.AdressParking }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ parking.Capacite }}</td>
            </tr>
            <tr>
                <th>Couvre_Soleil</th>
                <td>{{ parking.CouvreSoleil }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ parking.Mail }}</td>
            </tr>
            <tr>
                <th>Tele</th>
                <td>{{ parking.Tele }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parking_index') }}">back to list</a>

    <a href="{{ path('app_parking_edit', {'id': parking.id}) }}">edit</a>

    {{ include('parking/_delete_form.html.twig') }}
{% endblock %}
